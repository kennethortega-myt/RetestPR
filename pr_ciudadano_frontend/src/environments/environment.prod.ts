import { BICAMERALIDAD } from '../app/settings/procesos-electorales.settings';

export const environment = {
  production: true,
  apiUrlLocal: '/presentacion-backend/',
  procesoConfig: BICAMERALIDAD
};
